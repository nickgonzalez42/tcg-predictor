<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- Matches the nav logo: a white trading-card outline. On a dark rounded
       square so it stays visible on light browser tabs (the same white-on-dark
       look the logo has in the app). -->
  <rect width="64" height="64" rx="12" fill="#0b0f19"/>
  <rect x="16.5" y="10" width="31" height="44" rx="4"
        fill="none" stroke="#ffffff" stroke-width="3.5"/>
</svg>
